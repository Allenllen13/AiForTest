server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: ai-for-test
  
  # 静态资源配置
  web:
    resources:
      static-locations: classpath:/static/
      add-mappings: true

# 飞书API配置（可选，用于获取飞书文档内容）
feishu:
  app:
    id: 
    secret: 
  enabled: true

# 大模型配置
volc:
  ark:
    enabled: true # 默认关闭大模型调用，避免未配置导致报错
# 0. 火山引擎配置
    base-url: https://ark.cn-beijing.volces.com/api/v3
    api-key: 
#    model: doubao-seed-1-6-251015 #四分钟返回九十个
#    model: doubao-seed-1-6-flash-250828 #两分半101条
    model: deepseek-v3-1-terminus

# 1. OpenAI 配置示例
#  api-url: https://api.openai.com/v1/chat/completions
#  api-key: sk-你的OpenAI密钥（替换为实际密钥）
#  model: gpt-4o

  # 2. 智谱 AI 配置示例（替换上面3行即可）
  # api-url: https://open.bigmodel.cn/api/paas/v4/chat/completions
  # api-key: 你的智谱AI密钥（替换为实际密钥）
  # model: glm-4

  # 3. 通义千问配置示例（阿里云）
  # api-url: https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation
  # api-key: 你的阿里云密钥
  # model: qwen-turbo

logging:
  level:
    com.example: INFO
    org.springframework: WARN


